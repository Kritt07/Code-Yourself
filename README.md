## 1) Описание игры (концепция)
**Code Yourself** — пошаговая головоломка с элементами платформера, где игрок **не управляет напрямую**, а пишет набор команд (псевдокод), которые затем выполняются автоматически.

Игрок управляет квадратным персонажем. Цель — пройти уровень, полный препятствий (пилы, движущиеся объекты, ловушки), используя заранее прописанный алгоритм действий.

Ключевая идея: игрок должен **не реагировать**, а **предугадывать** и **программировать** решение.

Каждый уровень — это задача на:
- логику
- тайминг
- оптимизацию команд

## 2) Основные механики

### 2.1) Пошаговая система (Tick-based)
- Игра работает по тикам (шагам времени) и разделяет **логические тики (simulation)** и **рендер (render)**.
- **1 command tick = 0.5 секунды**.
- **1 команда** исполняется мгновенно (задаёт намерение движения), а затем проигрывается **30 simulation ticks** (это и есть один command tick).
- На каждом simulation tick:
  - обновляются препятствия (детерминированно от индекса тика)
  - обновляется физика игрока
  - проверяются столкновения (включая swept-проверку для опасностей)

### 2.2) Программирование действий
- Игрок пишет команды в текстовом поле и нажимает **Run**.
- Во время выполнения на каждом тике подсвечивается текущая выполняемая команда. Это помогает:
  - понимать поведение персонажа
  - находить ошибки
  - обучаться логике игры

### 2.3) Система команд
Базовые команды:
- `MOVE LEFT n`
- `MOVE RIGHT n`
  - задаёт горизонтальное намерение так, чтобы за один command tick игрок **стремился пройти 50px** (с учётом коллизий)
- `JUMP LEFT n`
- `JUMP RIGHT n`
  - прыжок работает через **импульс** (вертикальная скорость вверх) и доступен только когда игрок стоит на земле/платформе
  - `n` — горизонтальная дальность в клетках, `1..3` (иначе ошибка)
- `WAIT n`
  - ожидание `n` тиков

Циклы:
- `REPEAT n`
  - блок команд
- `END`
  - закрытие блока

Повтор блока команд `n` раз предполагается реализовать через **стек**.

### 2.4) Ограничения
- лимит на количество строк (например, 5–10)
- ограниченное количество команд

### 2.5) Препятствия (паттерны)
Все препятствия работают по детерминированным паттернам и синхронизированы с тик‑системой.

Примеры:
- пила (движется туда‑обратно)
- платформа (static / moving)

### 2.6) Простая физика (vx/vy + гравитация)
Физика реализована как классический платформер:
- `vx`/`vy` — скорости в пикселях за simulation tick
- `gravity` добавляется к `vy` каждый simulation tick (с ограничением `maxFallSpeed`)
- коллизии решаются раздельно по осям (**X**, затем **Y**)
- **moving-platform ride**: если игрок стоит на движущейся платформе (grounded на ней), он дополнительно смещается на `platformDx` за тик

Дефолтные параметры (под 30 Гц симуляцию):
- MOVE: 50px / command tick (30 sim ticks), распределяется детерминированно по sim ticks
- `gravity`: ~1.4 px/tick^2
- `jumpImpulse`: ~20 px/tick (вверх)
- `maxFallSpeed`: ~30 px/tick (вниз)

### 2.7) Плавность (визуал)
Логика дискретная (по тикам), но визуал должен быть плавным. Реализуется через интерполяцию:
- позиции между тиками анимируются
- создаётся ощущение “живого” движения

## 3) Управление и интерфейс
Игровое:
- **Run** — запустить выполнение
- **Reset** — перезапустить уровень

Интерфейс/макет:
- левый блок — редактор команд
- правый блок — игровое поле

## 4) Архитектура (высокоуровнево)
Сущности/модули (названия могут отличаться в проекте, но смысл такой):
- **Command** — базовый класс/тип команд
- **Parser** — разбор текста в структуру команд (включая вложенные блоки `REPEAT`)
- **Executor** — выполнение команд по тикам (курсор текущей команды, обработка `n`, стек повторов)
- **GameState** — текущее состояние мира (позиции, объекты)
- **Renderer** — отрисовка/анимация (интерполяция между тиками)

## 5) План разработки

### Неделя 1
Цель: сделать “игра вообще работает”.

Обязательно:
- [x] окно приложения
- [x] базовая отрисовка (персонаж)
- [x] тик‑система
- [x] простое движение (без команд)

Результат: персонаж двигается по тикам.

### Неделя 2
Цель: реализовать программирование.

Задачи:
- [x] парсер команд
- [x] выполнение команд
- [x] UI для ввода
- [x] тесты

Результат: игрок может пройти уровень через код.

### Неделя 3
Цель: геймплей.

Задачи:
- [x] препятствия
- [x] паттерны движения
- [x] столкновения
- [x] баланс тиков
- [x] тесты

### Неделя 4
Цель: контент и полировка.

Задачи:
- [x] сделать победу и поражение
- [X] сделать тему для игры
- [X] анимации

### Неделя 5
Задачи:
- [ ] уровни
- [ ] меню
- [ ] тестовый интерфейс игры

## 6) Definition of done (уровень/прогон)
- [x] Команды воспроизводимо выполняются по тикам (детерминизм).
- [x] Подсветка текущей команды соответствует фактическому исполнению.
- [x] Мир обновляется и проверяет столкновения на каждом тике.
- [x] `Reset` полностью возвращает состояние уровня к стартовому (включая паттерны препятствий и позицию персонажа).

